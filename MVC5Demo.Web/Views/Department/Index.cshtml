
@model IEnumerable<MVC5Demo.Web.Models.Department>

@{
    ViewBag.Title = "All Department";
}

<h2>@ViewBag.Title</h2>

<P>

  @Html.ActionLink("Create New", "Create")
</P>

<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelitem =>item.Name)

            </td>
            <td>
                @Html.ActionLink("Editx", "Edit", new  { id = item.Id}) |
                @Html.ActionLink("Detailsx", "Detail", new  { id = item.Id})|
                @Html.ActionLink("Deletex", "Delete", new   {  id = item.Id})

                @*@Html.ActionLink(item.Name, "details", "department", new { item.Id }, null)*@
            </td>
        </tr>
        
    }


</table>